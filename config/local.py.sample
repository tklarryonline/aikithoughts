from config.common import Config


class LocalConfig(Config):
    SECRET_KEY = 'FIXME'
    DEVELOPMENT = True
    DEBUG = True
    TESTING = True

    # Settings for SQLAlchemy
    SQLALCHEMY_DATABASE_URI = "FIXME"

    # Settings for Flask Security
    SECURITY_PASSWORD_HASH = "plaintext"
